<ul>
  <% if Admin::InstructorsController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_instructors_url
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_instructor_url,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::InstructorsController.sortable? and Instructor.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Instructors"),
                 admin_instructors_url,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Instructors"),
                 admin_instructors_url,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
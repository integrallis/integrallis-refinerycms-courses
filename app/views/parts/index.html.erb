<% content_for :body_content_left do %>
  <ul id="parts">
    <% @parts.each do |part| %>
      <li>
        <%= link_to part.title, part_url(part) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render :partial => "/shared/content_page" %>

<ul>
  <% if Admin::VenuesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_venues_url
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_venue_url,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::VenuesController.sortable? and Venue.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Venues"),
                 admin_venues_url,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Venues"),
                 admin_venues_url,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>
<ul>
  <% if Admin::AttendeesController.searchable? %>
    <li>
      <%= render :partial => "/shared/admin/search",
                  :locals => {
                    :url => admin_attendees_url
                  } %>
    </li>
  <% end %>
  <li>
    <%= link_to t('.create_new'), new_admin_attendee_url,
                 :class => "add_icon" %>
  </li>
<% if !searching? and Admin::AttendeesController.sortable? and Attendee.count > 1 %>
  <li>
    <%= link_to t('.reorder', :what => "Attendees"),
                 admin_attendees_url,
                 :id => "reorder_action",
                 :class => "reorder_icon" %>

    <%= link_to t('.reorder_done', :what => "Attendees"),
                 admin_attendees_url,
                 :id => "reorder_action_done",
                 :style => "display: none;",
                 :class => "reorder_icon" %>
  </li>
<% end %>
</ul>